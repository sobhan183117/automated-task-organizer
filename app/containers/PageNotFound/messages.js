/*
 * PageNotFound Messages
 *
 * This contains all the text for the PageNotFound container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.PageNotFound";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PageNotFound container!"
  }
});
