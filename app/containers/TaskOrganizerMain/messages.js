/*
 * TaskOrganizerMain Messages
 *
 * This contains all the text for the TaskOrganizerMain container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.TaskOrganizerMain";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the TaskOrganizerMain container!"
  }
});
